const nomes = ["calabreso", "linguiço", "fernando", "luiso", "reginaldo", "donald",];

function aleatorio(lista){
  const posicao = Math.floor(Math.random()* listaa.length);
  return lista[posicao];
}

const nome = aleatorio(nomes)
